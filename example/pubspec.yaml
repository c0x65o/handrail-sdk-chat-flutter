name: handrail_message_timeline_lab
description: Browser-observable HandrailMessageTimeline acceptance fixture.
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  web: ^1.1.1
  handrail_chat:
    git:
      url: https://github.com/c0x65o/handrail-sdk-chat-flutter.git
      ref: 51bc3e1411858ce38980f5beded683dee957d1a3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
